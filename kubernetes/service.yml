apiVersion: v1
metadata:
 name: pet-shop-security-service
kind: Service
spec:
  selector:
   app: pet-shop-security
  ports:
  - name: ps-sec-port
    port: 8080
    targetPort: ps-sec-port
  selector:  #service para o pod com o label app helloworld
    app: sca-eureka-deployment #service para o pod com o label app helloworld
   
